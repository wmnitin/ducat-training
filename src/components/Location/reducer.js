

const initialState = {
  latitude: null,
  longitude: null,
  location_suggestions: null,
  searchVal: '',
  entity_id: null
}

export default function (state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}